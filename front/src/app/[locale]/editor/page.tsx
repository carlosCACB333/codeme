import { title } from "@/components/assets/primitives";

export default function Docs() {
  return (
    <div className="flex items-center justify-center h-full w-full">
      <h3 className={title({ color: "green" })}>Select a project</h3>
    </div>
  );
}
